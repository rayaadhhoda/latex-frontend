{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Spartan Write (CodeLLDB)",
      "type": "lldb",
      "request": "attach",
      "pid": "${command:pickMyProcess}",
      "sourceLanguages": ["rust"]
    }
  ]
}
