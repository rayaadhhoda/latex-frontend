{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "log:default",
    "opener:default",
    "dialog:allow-open",
    "shell:allow-open",
    "deep-link:default",
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        {
          "name": "bin/spartan-write-sidecar",
          "sidecar": true
        }
      ]
    }
  ]
}
